name = "kumarmagnacity"
compatibility_date = "2024-03-09"
pages_build_output_dir = "."

[env.production]
name = "kumarmagnacity"
# Route to custom domain handled via Cloudflare Dashboard
# This config ensures Wrangler always targets the correct project
